import type {Metadata} from "next";import {HubNav} from "@/app/components/site/hub-nav";import {OpportunitiesHub} from "@/app/components/site/opportunities-hub";import {readSiteContent} from "@/app/lib/content-store";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Opportunities | ColorStackRUN",description:"Reviewed internships, scholarships, fellowships, and student opportunities shared by ColorStackRUN.",alternates:{canonical:"/opportunities"}};export default async function Page(){const content=await readSiteContent();return <main className="min-h-screen bg-[#fafafa] dark:bg-[#080808]"><HubNav siteLinks={content.links}/><OpportunitiesHub content={content}/></main>}
