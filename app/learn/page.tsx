import type {Metadata} from "next";import {HubNav} from "@/app/components/site/hub-nav";import {LearningHub} from "@/app/components/site/learning-hub";import {readSiteContent} from "@/app/lib/content-store";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"ColorStackRUN Learning Hub",description:"Whiteboard Warriors recordings and learning resources from ColorStackRUN.",alternates:{canonical:"/learn"}};export default async function Page(){const content=await readSiteContent();return <main className="min-h-screen bg-[#fafafa] dark:bg-[#080808]"><HubNav siteLinks={content.links}/><LearningHub content={content}/></main>}
